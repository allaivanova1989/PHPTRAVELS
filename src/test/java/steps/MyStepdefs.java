package steps;

public class MyStepdefs {
    public MyStepdefs() {
        Given("^user on the offers page$", () -> {
        });
        When("^user$", () -> {
        });
        Then("^user can see all offers$", () -> {
        });
    }

    private void Given(String s, Object o) {
    }
}
